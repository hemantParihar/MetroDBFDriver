# GitHub Pages (Jekyll) configuration.
# Enable Pages in repo Settings -> Pages -> Source: "Deploy from a branch",
# Branch: main, Folder: /docs.
title: DBF-JDBC-Driver
description: >-
  A pure-Java JDBC driver for DBF (dBASE / FoxPro / Clipper xBase) files —
  SQL queries, joins, aggregates, a rich function library, transactions,
  multi-writer locking and Clipper .NTX index read/write.
remote_theme: pages-themes/cayman@v0.2.0
plugins:
  - jekyll-remote-theme
show_downloads: true
